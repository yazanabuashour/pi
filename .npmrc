# npm disables non-registry dependency types by default. The TypeScript policy
# dev dependencies are commit-pinned GitHub sources without registry
# publication; allow Git specs from the root manifest only.
allow-git=root
